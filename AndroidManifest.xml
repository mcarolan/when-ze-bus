<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.mcarolan.whenzebus"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    
    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".WhenZeBusApplication">
        <activity
            android:name=".BusView" />
        <activity
            android:name=".BusStopView"
            android:label="@string/busstoplist_selectstop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
        <activity
            android:name=".ManageStops"
            android:label="@string/manage_stops" />
        <activity
            android:name=".AddView"
            android:label="@string/add" />
    </application>

</manifest>
